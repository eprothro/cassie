RSpec.describe Cassie::Schema::Migration do
  let(:klass){ Cassie::Schema::Migration  }
  let(:object) { klass.new(version) }
  let(:version){ nil }

  describe "version" do
    it "builds version from name" do
    end
  end

  describe "comparing" do
    it "compares to migration" do

    end
    it "compares to version" do

    end
    it "compares to string" do

    end
  end
end
